# Ruby toolchain for the iOS TestFlight pipeline (fastlane + CocoaPods).
# Used by `bundle exec fastlane ios beta` locally and by .github/workflows/ios.yml.
# Only relevant on macOS / CI — desktop and Android builds don't need this.
source "https://rubygems.org"

gem "fastlane"
gem "cocoapods"
